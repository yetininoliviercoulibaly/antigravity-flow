## NestJS Specific Rules

1.  **Dependency Injection**: Always use DI for service dependencies.
2.  **Modules**: Organize code into modules by feature (e.g., `AuthModule`, `UserModule`).
3.  **DTOs**: Use DTOs for data validation with `class-validator`.
